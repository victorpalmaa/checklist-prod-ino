export function Login() {
  return (
    <div className="min-h-screen w-full flex items-center justify-center bg-[var(--color-surface-page)] px-4">
      <div className="w-full max-w-sm">
        <h1 className="text-display">Entrar</h1>
        <p className="text-body text-[var(--color-fg-secondary)] mt-2">
          Tela de login a ser implementada na task C2 — autenticação via Supabase.
        </p>
      </div>
    </div>
  );
}
